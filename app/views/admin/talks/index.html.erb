<h1>Listing talks</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @talks.each do |talk| %>
  <tr>
    <td><%= talk.title %></td>
    <td><%= talk.description %></td>
    <td><%= talk.start_time %></td>
    <td><%= talk.end_time %></td>
    <td><%= talk.status %></td>
    <td><%= link_to 'Show', talk %></td>
    <td><%= link_to 'Edit', edit_admin_talk_path(talk) %></td>
    <td><%= link_to 'Destroy', talk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Talk', new_admin_talk_path %>
